<idea-plugin>
    <id>com.coderevolt.super-hotswap</id>
    <name>SuperHotSwap</name>
    <vendor email="646997146@qq.com" url="https://songbiaoself.github.io/">CodeRevolt</vendor>

    <description>It aims to realize the most convenient IDEA hot update plug-in, reduce user operation steps, and
        provide zero-configuration visual operation updates. Currently, hot updates of MapperXML and Java files are
        supported. The details are similar to update records. email: 646997146@qq.com
    </description>

    <!-- please see https://plugins.jetbrains.com/docs/intellij/plugin-compatibility.html
         on how to target different products -->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <!-- Add your extensions here -->
        <!--notify 2020.3版本后需在plugin中注册-->
        <notificationGroup displayType="BALLOON" id="notifyAction"/>
        <fileEditorProvider implementation="com.coderevolt.ui.ClassFileEditorProvider"/>
    </extensions>

    <projectListeners>
        <listener class="com.coderevolt.listener.ProjectExecutionListener"
                  topic="com.intellij.execution.ExecutionListener"/>
        <listener class="com.coderevolt.listener.MyApplicationListener"
                  topic="com.intellij.ide.AppLifecycleListener"/>
    </projectListeners>

    <actions>
        <!--            id 全局唯一-->
        <!-- popup属性：决定该Group是否允许子菜单     -->
        <!-- icon：图标，需要配置为全限定命名    -->
        <!-- 不配置compact属性，表示该Group需要支持子菜单    -->
        <group
                id="FileHotswapAction"
                class="com.coderevolt.action.FileHotSwapGroupAction"
                text="File Hot Swap"
                popup="true">
            <add-to-group group-id="EditorPopupMenu" anchor="first"/>
            <add-to-group group-id="ChangesViewPopupMenu" anchor="first"/>
            <add-to-group group-id="ProjectViewPopupMenu" anchor="first"/>
        </group>

        <!-- 定义Action -->
        <action id="superHotSwapConfigAction" class="com.coderevolt.action.RemoteConfigAction" text="Super HotSwap" description="superHotSwap config">
            <!-- 将Action添加到Settings页面的Tools菜单下 -->
            <add-to-group group-id="ToolsMenu" anchor="first"/>
        </action>
    </actions>
</idea-plugin>